name: NUS_TEMP 
   
on:
 push: 
  branches: [ master ] 
 pull_request: 
  branches: [ master ] 
 schedule: 
 - cron: '2 4 * * *' 
 workflow_dispatch: [ master ] 
jobs: 
  build: 
   name: Build 
   runs-on: ubuntu-latest 
   steps: 
     - name: Check out code 
       uses: actions/checkout@v2 
     - name: run
       run:  python3 main.py
